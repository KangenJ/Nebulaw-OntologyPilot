# 本地检查

在项目根目录使用 Node.js 24+ 执行：

```sh
npm run test:smoke
```

这组测试无需安装依赖、连接远端或提供令牌。它检查部分界面、数据边界和选模逻辑，不启动完整平台。

需要构建依赖的拟合测试在未配置 `PLUS_BENCHMARK_ROOT` 时会跳过，不能视为完整模型验证。

完整平台的干净环境安装与统一启动仍在整理。根目录 `npm start` 是保留的旧版入口，不是当前公开版的一键启动方式。
